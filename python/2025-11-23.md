# 파이썬 개념 정리

1. 파이썬 소개

## <파이썬 특징>
- 문법이 직관적이고 간결하여 프로그램 로직을 연습하기 좋음
- 인터프리터 방식
1) 소스를 실행할 때 바로 기계어로 번역하여 실행하는 방식
2) 따로 실행파일이 없음
3) 장점 : 운영체제마다 따로 실행파일 만들 필요 없이 소스만 관리하면 되므	   로 유지보수가 편리함
4) 단점: 컴파일러 방식에 비해 실행 시간이 김
- 풍부한 라이브러리 : 그리기, 윈도우프로그램, 게임, 웹앱, ai, 빅데이터 분석

## <파이썬 제공 함수>
- print(“문자열”) : 화면에 문자열을 출력하는 함수
- 문자열 : “문자열”또는 ‘문자열’로 표시
- 문자열 관련 문법 :
   1) 문자열+문자열 -> 2개 문자열을 1개 문자열로 연결하기
   2) 문자열+숫자 -> 문자열을 숫자만큼 반복하기

## <터틀 그래픽>
import turtle as t : 터틀 모듈을 t라는 이름으로 불러오기
t.shape(“turtle”) : 펜촉을 거북이 모양으로 바꾸기
t.right(각도), t.left(각도) : 오른쪽, 왼쪽으로 회전
t.fd 또는 t.forward : 앞으로 이동
t.up() : 펜을 들어올려 이동 시 선을 그리지 않게 함
t.down() : 펜을 내려 선을 그릴 수 있도록 함
t.done() : 창을 닫지 않고 유지

2. 변수 소개

## <변수란?>
- 변수는 값을 저장하는 메모리 공간에 이름을 붙인 것, 컴퓨터에서는 변수를 사용하여 정보를 저장함

## <=기호>
- 파이썬에서 = 기호는 “변수에 값을 저장하라”라는 의미임
- 등호는 ==와 같이 표시함 

## <변수 이름 규칙>
- 의미 있는 이름 사용
- 소문자와 대문자는 서로 다르게 취급
- 영문자, 숫자, 밑줄(_) 사용 가능
- 숫자로 시작 불가
- 공백 및 특수문자 사용 불가

## <사용자로부터 정수 입력받는 함수>
x = int(input(“첫 번째 정수를 입력하시오: ”))
y = int(input("두 번째 정수를 입력하시오: "))
sum = x + y
print(x, "과", y, "의 합은", sum, "입니다.")

## <print() 함수>
변수와 문자열을 동시에 출력할 수 있음

3. 계산

## <연산자와 피연산자>
- 수식(expression) : 피연산자들과 연산자의 조합
- 연산자(operator) : 연산을 나타내는 기호
- 피연산자(operand) : 연산의 대상이 되는 값

## <산술연산자>
나눗셈 : 7//4 -> 결과값 : 1
	  7/4 -> 결과값 : 1.75

## <초값을 입력받아 시간, 분, 초로 변환한 값 출력하기>
# 사용자에게 초값을 입력받아 변수로 저장하기
time_value = int(input("변환할 초값을 입력하세요: "))
# 시간, 분, 초값으로 변환하기
hour = time_value // (60*60)
remainder = time_value % (60*60) #분,초값
min = remainder // 60
sec = remainder % 60
# 화면에 출력하기
print("입력값:", time_value, "은", hour, "시간", min, "분", sec, "초입니다")

## <할당 연산자>
다음과 같이 여러 개의 변수에 서로 다른 값을 저장하는 것도 가능하다.
x, y = 100, 200

4. 자료의 종류
## <파이썬과 자료형>
- 변수에 어떤 종류의 자료도 저장할 수 있음
x = 10
print("x =", x)
x = 3.14
print("x =", x)
x = "Hello World!"
print("x =", x)

## <문자열>
- 문자들의 나열로, 큰따옴표(" ") 또는 작은따옴표(' ')로 표현
- 문자열 안에 따옴표를 포함할 경우 다른 종류의 따옴표 사용
- int() : 문자열을 정수로 변환
- float() : 문자열을 실수로 변환
- str() : 숫자를 문자열로 변환

## <type() 함수>
- 자료형을 알려면 type() 함수를 사용

## <문자열 연산>
- 문자열을 반복하려면 -> *연산자
- 문자열의 길이 : len()

## <문자열에 변수값 포함 -> f-string>
>>> price = 10000
>>> print(f"상품의 가격은 {price}원입니다.")
상품의 가격은 10000원입니다.

## <리스트>
- 리스트는 여러 형태의 자료들을 배열로 저장한 것
- sample_list = [1, “hello”, 3.14]

5. 조건문

## <if-else 문>
- 조건식이 참이면 if 아래의 블록이 실행, 그렇지 않으면 else 아래의 블록이 실행됨
예제)
score = int(input("성적을 입력하시오: "))

if score >= 60:         # score가 60 이상이면
    print("합격입니다.")     # 합격이다. 
else:               # 그렇지 않으면
    print("불합격입니다.")    # 불합격이다. 

## <논리 연산자의 종류>
- x and y : AND 연산, x와 y가 모두 참이면 참, 그렇지 않으면 거짓
- x or y : OR 연산, x나 y중에서 하나만 참이면 참, 모두 거짓이면 거짓
- not x : NOT 연산, x가 참이면 거짓, x가 거짓이면 참

## <무한 반복 구조>
while True:
	...
	...
	...

## <random.randrange(n) 함수>
- 0 이상 n 미만의 정수 중 하나를 무작위로 반환

## <random.randint(a,b) 함수>
- a 이상 b 이하의 정수를 무작위로 하나 반환

6. 반복문

## <횟수 제어 반복(for 문)>
- 정해진 횟수만큼 반복 (횟수를 아는 경우)
예제1)
for i in [1,2,3,4,5]:		# 끝에 :이 있어야 함
   print(“방문을 환영합니다.”)	#들여쓰기해야 함
- i가 1부터 5까지 변경되면서 반복

for 변수 in range(종료 값):		#0에서 (종료 값-1)까지의 숫자를 반환함
   문장			#반복되는 문장으로 들여쓰기해야 함

예제2)
for i in range(1,6,1):	#1: 시작 값, 6: 종료 값(6은 포함X), 1: 증가 값
   print(i, end=“”)		#옆으로 계속 쓰라는 뜻

## <조건 제어 반복(while 문)>
- 특정한 조건을 만족하면 계속 반복 (횟수를 모르는 경우)
while 조건:		#반복을 하는 조건, 조건이 참이면 반복을 계속 함
	반복 문장	#반복되는 문장

while True:		#무한 반복
	반복 문장
	반복 문장
	if 조건:
	   break	#루프를 강제적으로 빠져 나올 때 사용

7. 함수

## <함수란?>
- 코드의 묶음에 이름을 붙인 것
- 입력을 받아서 출력을 내보내는 박스로 생각할 수 있다.
예제1)
#함수정의
def print_address(name):	#매개변수
   print(“서울특별시 종로구 1번지”)
   print(“파이썬 빌딩 7층”)
   print(name)
#함수 호출
print_address(“홍길동”)	#인수(인자)

- 결과값
서울특별시 종로구 1번지
파이썬 빌딩 7층
홍길동

## <변수의 종류>
- 지역 변수: 
 1) 함수 안에서 선언되는 변수
 2) 함수 안에서만 사용이 가능하다.
 3) 함수가 종료되면 없어진다.
- 전역 변수: 
 1) 함수 외부에서 선언되는 변수
 2) 어디서나 사용할 수 있다.
예제1)
def calculate_area ():
    result = 3.14 * r**2
    return result

r = float(input("원의 반지름: "))
area = calculate_area()
print(area)

## <재귀함수>
- 자기 자신을 호출하는 함수
예제1)
#4 팩토리얼 함수를 재귀함수로 정의하기
def calc_fact(n):
    if n == 1:
        return 1
    return calc_fact(n-1) *n

while True:
    n = int(input("1이상의 정수 입력(0 입력 시 종료): "))
    if n == 0:
        print("프로그램 종료합니다.")
        break
    print(f"{n}! = {calc_fact(n)}")

