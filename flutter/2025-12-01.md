# 🥕 [Flutter] 당근마켓 클론 프로젝트 (밤톨마켓) - 앱 구조 설계 및 초기 세팅

**📅 Date:** 2025.12.01
**🚀 Tech Stack:** Flutter, GetX, Firebase, Shared_Preferences
**📂 Project:** 밤톨마켓 (Bamtol Market App)

---

## 1. 오늘의 목표
- `GetX`를 활용한 상태 관리 및 라우팅 설정
- `Firebase` 연동 및 의존성 주입(Dependency Injection)
- `SharedPreferences`를 이용한 앱 최초 실행 여부 판단 (Onboarding)
- `BottomNavigationBar`를 활용한 메인 화면 뼈대 구축

---

## 2. 배운 내용 & 구현 상세

### 🏗 A. 메인 진입점과 의존성 주입 (`main.dart`)
앱의 시작점인 `main.dart`에서 `GetMaterialApp`을 사용하여 라우팅과 테마를 설정했다.
특히 `initialBinding`을 통해 앱 실행 시 필요한 Repository와 Controller들을 미리 메모리에 올려두는 방식을 사용하여, 어디서든 컨트롤러에 접근할 수 있게 구조를 잡았다.

* **Firebase 초기화:** `DefaultFirebaseOptions`를 통해 플랫폼별 설정을 자동화.
* **다크 모드 테마:** 당근마켓 특유의 배경색(`Color(0xff212123)`)을 `scaffoldBackgroundColor`로 전역 설정.
* **DI (의존성 주입):** `AuthenticationRepository`, `UserRepository`, `BottomNavController` 등을 `Get.put`으로 주입.

```dart
// main.dart 핵심 코드
initialBinding: BindingsBuilder(() {
  var authenticationRepository = AuthenticationRepository(FirebaseAuth.instance);
  Get.put(authenticationRepository);
  Get.put(BottomNavController()); // 바텀 네비게이션 컨트롤러 주입
  // ... 기타 컨트롤러 주입
}),