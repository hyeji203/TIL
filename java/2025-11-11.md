<details>
<summary>실습 코드 보기</summary>

```java
import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

public class ImageViewer extends JFrame{
	JButton btnfirst,btnlast,btnprev,btnnext;
	ImageIcon [] image;
	JLabel imageLabel, lblTitle;
	int cnt;
	String [] text= {"영국-런던브릿지","스위스-융프라우","이탈리아-트레비분수","파리-에펠탑"}; //문자열 배열 초기화
	
	ImageViewer(){
		setTitle("랜선으로 떠나는 여행");
		Font f=new Font("",Font.BOLD,30);
		
		JPanel panel=new JPanel();
		lblTitle=new JLabel("랜선으로 떠나는 여행",SwingConstants.CENTER);
		lblTitle.setFont(f);
		
		btnfirst=new JButton("<");
		btnlast=new JButton("<");
		btnprev=new JButton("<<");
		btnnext=new JButton(">>");
		
		btnprev.setFont(f);
		btnnext.setFont(f);
		
		//이미지 객체 배열 생성
		image=new ImageIcon[4];
		
		panel.setBackground(Color.pink);
		
		//이미지 로딩
		for(int i=0;i<image.length;i++) {
			String path="image/img"+i+".png";
			image[i]=new ImageIcon(path);
		}
		imageLabel=new JLabel(image[cnt]);
		
		//내부클래스로 리스너 구현
		
		//처음버튼
		class First implements ActionListener{
			@Override
			public void actionPerformed(ActionEvent e) {
				JOptionPane.showMessageDialog(null,"처음 이미지로 이동합니다.","경고",JOptionPane.WARNING_MESSAGE);
				imageLabel.setIcon(image[0]);
				lblTitle.setText(text[0]);
				}
		}
		//마지막버튼
		
		//이전버튼
		class Preview implements ActionListener{
			@Override
			public void actionPerformed(ActionEvent e) {
				if(cnt > 0)
					cnt--;
				else
					JOptionPane.showMessageDialog(null,"이전 그림 없음","경고",JOptionPane.WARNING_MESSAGE);
				
				imageLabel.setIcon(image[cnt]);
				lblTitle.setText(text[cnt]);
				}
		}
		//다음버튼
		class Next implements ActionListener{
			public void actionPerformed(ActionEvent e) {
				if(cnt < 3) 
					cnt++;
				else
					JOptionPane.showMessageDialog(null, "다음 그림 없음", "경고", JOptionPane.WARNING_MESSAGE);

				imageLabel.setIcon(image[cnt]);
				lblTitle.setText(text[cnt]);
			}
		}
		
		
		btnprev.addActionListener(new Preview());
		btnnext.addActionListener(new Next());
		add(lblTitle, "North");
		add(imageLabel,"Center");
		panel.add(btnprev);
		panel.add(btnnext);
		add(panel,"South");
		setSize(500,600);
		setVisible(true);
	}
	
	

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		new ImageViewer();
	}

}

```
</details>

# 📸 Java Swing 프로젝트: 랜선 여행 이미지 뷰어

> **주제:** Java GUI (Swing), 이벤트 처리, 내부 클래스  

## 1. 프로젝트 개요
`JFrame`과 `JButton`, `ImageIcon`을 활용하여 버튼을 클릭하면 여행지 사진과 제목이 변경되는 **이미지 뷰어** 프로그램입니다.

### 📌 주요 기능
* **이미지 출력:** 런던, 스위스, 이탈리아, 파리 4곳의 여행지 사진 표시
* **네비게이션:** 이전/다음 버튼을 통해 사진 변경
* **예외 처리:** 처음이나 마지막 사진에서 버튼 클릭 시 `JOptionPane` 경고창 출력

---

## 2. 핵심 구현 내용

### A. UI 구성 요소 (Components)

| 클래스 | 변수명 | 역할 |
| :--- | :--- | :--- |
| `JFrame` | - | 전체 윈도우 창 (컨테이너) |
| `JPanel` | `panel` | 하단 버튼들을 묶어서 배치하는 패널 |
| `JLabel` | `imageLabel`, `lblTitle` | 이미지와 텍스트(제목)를 출력하는 공간 |
| `JButton` | `btnprev`, `btnnext` | 이전/다음 기능을 수행하는 버튼 |
| `ImageIcon` | `image[]` | 이미지 파일을 메모리에 로드하여 저장하는 객체 배열 |

### B. 레이아웃 (Layout)
`BorderLayout`을 기본으로 사용하여 컴포넌트를 배치했습니다.
* **North:** `lblTitle` (여행지 제목)
* **Center:** `imageLabel` (사진)
* **South:** `panel` (이전/다음 버튼 포함)

```java
add(lblTitle, "North");
add(imageLabel, "Center");
panel.add(btnprev);
panel.add(btnnext);
add(panel, "South");
````

-----

## 3. 주요 로직 분석

### 1. 이미지 및 데이터 배열 처리

```java
// 제목 배열
String [] text = {"영국-런던브릿지", "스위스-융프라우", "이탈리아-트레비분수", "파리-에펠탑"};

// 이미지 로딩 반복문
image = new ImageIcon[4]; // 배열 생성
for(int i=0; i<image.length; i++) {
    // 파일 경로 동적 생성 (image/img0.png, img1.png ...)
    String path = "image/img" + i + ".png"; 
    image[i] = new ImageIcon(path);
}
```

> **Point:** 파일명을 `img0`, `img1` 처럼 규칙성 있게 만들어서 `for`문으로 한 번에 로딩한 것이 핵심

### 2\) 이벤트 리스너 (Event Listener) - 내부 클래스

#### '이전' 버튼 로직 (`Preview` 클래스)

현재 인덱스(`cnt`)가 0보다 클 때만 감소시키고, 아니면 경고창을 띄웁니다.

```java
class Preview implements ActionListener {
    @Override
    public void actionPerformed(ActionEvent e) {
        if(cnt > 0) {
            cnt--; // 인덱스 감소
        } else {
            // 경고창 출력
            JOptionPane.showMessageDialog(null, "이전 그림 없음", "경고", JOptionPane.WARNING_MESSAGE);
        }
        // 변경된 인덱스에 맞춰 이미지와 제목 갱신
        imageLabel.setIcon(image[cnt]);
        lblTitle.setText(text[cnt]);
    }
}
```

#### '다음' 버튼 로직 (`Next` 클래스)

현재 인덱스(`cnt`)가 3(마지막 인덱스)보다 작을 때만 증가시킵니다.

```java
class Next implements ActionListener {
    public void actionPerformed(ActionEvent e) {
        if(cnt < 3) {
            cnt++; // 인덱스 증가
        } else {
            JOptionPane.showMessageDialog(null, "다음 그림 없음", "경고", JOptionPane.WARNING_MESSAGE);
        }
        imageLabel.setIcon(image[cnt]);
        lblTitle.setText(text[cnt]);
    }
}
```

-----

## 4\. 배운 점 (Retrospective)

1.  **배열과 인덱스 관리:** `cnt` 변수 하나로 이미지 배열과 텍스트 배열을 동시에 컨트롤하는 방법을 익혔다.
2.  **리소스 경로:** `new ImageIcon("path")`를 사용할 때 파일 경로가 정확해야 이미지가 뜬다는 점을 주의해야 한다.
3.  **내부 클래스:** 리스너를 별도 파일이 아닌 클래스 내부에 작성하여, 외부 클래스의 멤버 변수(`imageLabel`, `cnt` 등)에 쉽게 접근할 수 있었다.